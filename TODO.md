 backend:
 - add passepartout
 
 frontend:
 - Design.tsx => Home.tsx - done
 - check in production that navigate(-1) from custom useNavigate hook (EventEdit, cancel), arriving from an external domain, sends to our domain home page, and does not exit to external domain ...
